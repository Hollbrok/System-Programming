#include "libs.h"
#include "debug.h"
#include "common.h"


int main(int argc, const char *argv[])
{
    if (argc < 2)
        err(EX_USAGE, "program needs name-of-file argument");



    exit(EXIT_SUCCESS);
}